{
 "name": "MaintenanceConfigurationProperties",
 "fields": [
  {
   "containsDiscriminatedTypeValue": false,
   "jsonName": "notAllowedTime",
   "name": "NotAllowedTime",
   "objectDefinition": {
    "type": "List",
    "referenceName": null,
    "nestedItem": {
     "type": "Reference",
     "referenceName": "TimeSpan"
    }
   },
   "optional": true,
   "required": false,
   "description": "Time slots on which upgrade is not allowed."
  },
  {
   "containsDiscriminatedTypeValue": false,
   "jsonName": "timeInWeek",
   "name": "TimeInWeek",
   "objectDefinition": {
    "type": "List",
    "referenceName": null,
    "nestedItem": {
     "type": "Reference",
     "referenceName": "TimeInWeek"
    }
   },
   "optional": true,
   "required": false,
   "description": "If two array entries specify the same day of the week, the applied configuration is the union of times in both entries."
  }
 ]
}