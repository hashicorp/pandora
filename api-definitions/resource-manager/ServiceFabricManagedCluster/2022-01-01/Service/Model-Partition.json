{
 "name": "Partition",
 "fields": [
  {
   "containsDiscriminatedTypeValue": true,
   "jsonName": "partitionScheme",
   "name": "PartitionScheme",
   "objectDefinition": {
    "type": "Reference",
    "referenceName": "PartitionScheme"
   },
   "optional": false,
   "required": true,
   "description": "Specifies how the service is partitioned."
  }
 ],
 "typeHintIn": "PartitionScheme"
}