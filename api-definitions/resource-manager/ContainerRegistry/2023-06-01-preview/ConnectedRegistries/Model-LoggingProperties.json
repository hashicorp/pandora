{
 "name": "LoggingProperties",
 "fields": [
  {
   "containsDiscriminatedTypeValue": false,
   "jsonName": "auditLogStatus",
   "name": "AuditLogStatus",
   "objectDefinition": {
    "type": "Reference",
    "referenceName": "AuditLogStatus"
   },
   "optional": true,
   "required": false,
   "description": "Indicates whether audit logs are enabled on the connected registry."
  },
  {
   "containsDiscriminatedTypeValue": false,
   "jsonName": "logLevel",
   "name": "LogLevel",
   "objectDefinition": {
    "type": "Reference",
    "referenceName": "LogLevel"
   },
   "optional": true,
   "required": false,
   "description": "The verbosity of logs persisted on the connected registry."
  }
 ]
}