{
 "name": "OutputWatermarkProperties",
 "fields": [
  {
   "containsDiscriminatedTypeValue": false,
   "jsonName": "maxWatermarkDifferenceAcrossPartitions",
   "name": "MaxWatermarkDifferenceAcrossPartitions",
   "objectDefinition": {
    "type": "String",
    "referenceName": null
   },
   "optional": true,
   "required": false,
   "description": "Describes the maximal delta between the fastest and slowest partitions, so the out of order window that catches all necessary events in downstream jobs is well defined."
  },
  {
   "containsDiscriminatedTypeValue": false,
   "jsonName": "watermarkMode",
   "name": "WatermarkMode",
   "objectDefinition": {
    "type": "Reference",
    "referenceName": "OutputWatermarkMode"
   },
   "optional": true,
   "required": false,
   "description": "The output watermark mode."
  }
 ]
}