{
 "name": "DeltaSerializationProperties",
 "fields": [
  {
   "containsDiscriminatedTypeValue": false,
   "jsonName": "deltaTablePath",
   "name": "DeltaTablePath",
   "objectDefinition": {
    "type": "String",
    "referenceName": null
   },
   "optional": false,
   "required": true,
   "description": "Specifies the path of the Delta Lake table that the output will be written to."
  },
  {
   "containsDiscriminatedTypeValue": false,
   "jsonName": "partitionColumns",
   "name": "PartitionColumns",
   "objectDefinition": {
    "type": "List",
    "referenceName": null,
    "nestedItem": {
     "type": "String",
     "referenceName": null
    }
   },
   "optional": true,
   "required": false,
   "description": "Specifies the names of the columns for which the Delta Lake table will be partitioned. We are only supporting 1 partition column, but keeping it as an array for extensibility."
  }
 ]
}